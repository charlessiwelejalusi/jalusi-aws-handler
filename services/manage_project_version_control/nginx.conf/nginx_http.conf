# Multi-Subdomain Nginx Configuration
# HTTP-only configuration
# Supports subdomain-based routing to different services
# - esdt.jalusitech.co.za -> http://127.0.0.1:8000/
# - agritech.jalusitech.co.za -> http://127.0.0.1:5001/

# Global settings
gzip on;
gzip_vary on;
gzip_min_length 1024;
gzip_proxied any;
gzip_comp_level 6;
gzip_types
    text/plain
    text/css
    text/xml
    text/javascript
    application/json
    application/javascript
    application/xml+rss
    application/atom+xml
    image/svg+xml;

# ============================================================================
# Server Block 1: ESDT Subdomain
# Handles requests to esdt.jalusitech.co.za
# Routes to backend service on port 8000
# ============================================================================
server {
    listen 80;
    server_name esdt.jalusitech.co.za;

    # Allow larger uploads through the proxy
    client_max_body_size 500M;

    # Route all requests to ESDT service
    location / {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Hide security headers that only work over HTTPS (prevents browser warnings)
        proxy_hide_header Cross-Origin-Opener-Policy;
        proxy_hide_header Cross-Origin-Embedder-Policy;
        proxy_hide_header Cross-Origin-Resource-Policy;
        proxy_hide_header Strict-Transport-Security;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Serve static files
    location /static/ {
        alias /var/www/html/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
        try_files $uri $uri/ @backend;
        
        # Ensure proper MIME types are set
        default_type application/octet-stream;
        types {
            text/css css;
            text/javascript js;
            application/javascript js;
            application/json json;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
    }
    
    # Fallback to backend if static file not found
    location @backend {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve media files
    location /media/ {
        alias /var/www/html/mediafiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# ============================================================================
# Server Block 2: Agritech Subdomain
# Handles requests to agritech.jalusitech.co.za
# Routes to backend service on port 5001
# ============================================================================
server {
    listen 80;
    server_name agritech.jalusitech.co.za;

    # Allow larger uploads through the proxy
    client_max_body_size 500M;

    # Route all requests to Agritech service
    location / {
        proxy_pass http://127.0.0.1:5001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Hide security headers that only work over HTTPS (prevents browser warnings)
        proxy_hide_header Cross-Origin-Opener-Policy;
        proxy_hide_header Cross-Origin-Embedder-Policy;
        proxy_hide_header Cross-Origin-Resource-Policy;
        proxy_hide_header Strict-Transport-Security;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Serve static files
    location /static/ {
        alias /var/www/html/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
        try_files $uri $uri/ @backend;
        
        # Ensure proper MIME types are set
        default_type application/octet-stream;
        types {
            text/css css;
            text/javascript js;
            application/javascript js;
            application/json json;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
    }
    
    # Fallback to backend if static file not found
    location @backend {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve media files
    location /media/ {
        alias /var/www/html/mediafiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:5001/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}

# ============================================================================
# Server Block 3: IP Address Access (Fallback)
# Replace IP_ADDRESS with your actual server IP address
# This block handles direct IP address access (e.g., http://13.245.234.27)
# Routes to default service on port 5001
# ============================================================================
server {
    listen 80 default_server;
    server_name IP_ADDRESS;

    # Allow larger uploads through the proxy
    client_max_body_size 500M;

    # Route to default web service
    location / {
        proxy_pass http://127.0.0.1:5001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Hide security headers that only work over HTTPS (prevents browser warnings)
        proxy_hide_header Cross-Origin-Opener-Policy;
        proxy_hide_header Cross-Origin-Embedder-Policy;
        proxy_hide_header Cross-Origin-Resource-Policy;
        proxy_hide_header Strict-Transport-Security;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Serve static files
    location /static/ {
        alias /var/www/html/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
        try_files $uri $uri/ @backend;
        
        # Ensure proper MIME types are set
        default_type application/octet-stream;
        types {
            text/css css;
            text/javascript js;
            application/javascript js;
            application/json json;
            image/svg+xml svg;
            image/png png;
            image/jpeg jpg jpeg;
            image/gif gif;
            image/webp webp;
            font/woff woff;
            font/woff2 woff2;
            font/ttf ttf;
            font/otf otf;
        }
    }
    
    # Fallback to backend if static file not found
    location @backend {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve media files
    location /media/ {
        alias /var/www/html/mediafiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://127.0.0.1:5000/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Error pages
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;
    
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}


# ============================================================================
# Configuration Notes:
# ============================================================================
# Subdomain Routing:
# - esdt.jalusitech.co.za -> http://127.0.0.1:8000/ (ESDT service)
# - agritech.jalusitech.co.za -> http://127.0.0.1:5001/ (Agritech service)
# - IP address access -> http://127.0.0.1:5001/ (default service)
#
# DNS Requirements:
# - Ensure DNS A records are configured for both subdomains pointing to server IP
# - esdt.jalusitech.co.za -> A record -> Server IP
# - agritech.jalusitech.co.za -> A record -> Server IP
#
# Configuration:
# 1. The script will automatically replace IP_ADDRESS with the instance IP address
# 2. Port numbers can be adjusted if services run on different ports
# 3. Each subdomain has its own server block with dedicated routing
# 4. Static and media file paths can be customized per subdomain if needed
# 5. Health check endpoints use the same port as the main service
# 6. Security headers (COOP, COEP, CORP, HSTS) are hidden over HTTP to prevent browser warnings
#    These headers only work over HTTPS and will be properly configured in HTTPS configs
#
# Testing:
# - After deployment, test each subdomain:
#   curl -H "Host: esdt.jalusitech.co.za" http://IP_ADDRESS/
#   curl -H "Host: agritech.jalusitech.co.za" http://IP_ADDRESS/
# ============================================================================
